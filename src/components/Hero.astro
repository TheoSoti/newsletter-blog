---
import { Picture } from 'astro:assets';
import profile from '../assets/profile.png';
---

<div class="hero">
	<div class="container">
		<Picture
			src={profile}
			formats={['avif', 'webp', 'jpg']}
			class="profile"
			width="440"
			height="440"
			alt="Theo Soti profile picture"
		/>

		<div class="text">
			<h1 class="title">I share CSS tricks for building a better web</h1>

			<p>
				Hi there, I am Theodore I am a freelance front-end developer. I am committed to sharing my web design knowledge,
				with a special focus on CSS. <br /><br />Here's what you can expect from this blog:
			</p>
			<ul>
				<li>In-depth articles that break down complex CSS</li>
				<li>Easy-to-understand explanations for all skill levels</li>
				<li>Ideas and tips to help you push the boundaries of design with code</li>
			</ul>
		</div>
	</div>
</div>

<style>
	.hero {
		border-bottom: 4px solid var(--border);
	}
	.container {
		width: 1100px;
		max-width: calc(100% - 2em);
		margin: auto;
		padding: 2em 0;
		display: flex;
		flex-direction: column;
	}
	.container > picture {
		flex-shrink: 0;
	}
	.profile {
		border-radius: 50%;
		border: 2px solid var(--border);
		flex-shrink: 0;
		transition: width 0.3s ease;
		width: clamp(60px, 60px + 10vw, 200px);
	}
	ul {
		margin: 0;
	}
	@media (min-width: 600px) {
		.container {
			padding: 3em 0;
			flex: 0 0 auto;
			justify-content: center;
			column-gap: 20px;
		}

		.profile {
			display: block;
		}
	}

	@media (min-width: 1024px) {
		.container {
			flex-direction: row;
			align-items: center;
		}
		.profile {
			margin-top: 10px;
		}
	}
</style>
