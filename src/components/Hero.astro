---
import { Picture } from 'astro:assets';
import profile from '../assets/profile.png';
---

<div class="hero">
	<div class="container">
		<Picture
			src={profile}
			formats={['avif', 'webp', 'jpg']}
			class="profile"
			width="440"
			height="440"
			alt="Theo Soti profile picture"
		/>

		<div class="text">
			<h1 class="title">Master CSS & Front-End Development</h1>
			<h2>Learn to build Stunning Web Interfaces</h2>
			<p>
				Hi there! My name is Theo Soti, a designer and front-end developer passionate about CSS and crafting beautiful,
				user-friendly interfaces.
			</p>

			<p>
				I work with businesses to create stunning websites and apps with a focus on responsive design, modern CSS, and
				pixel-perfect layouts.
			</p>

			<p>I also write about the web, helping developers of all levels master the art of front-end development.</p>
		</div>
	</div>
</div>

<style>
	.hero {
		border-bottom: 4px solid var(--border);
	}
	.container {
		width: 1100px;
		max-width: calc(100% - 2em);
		margin: auto;
		padding: 2em 0;
		display: flex;
		flex-direction: column;
	}
	h2 {
		margin-top: 0;
		font-weight: 400;
	}
	p {
		margin-bottom: 1em;
	}
	.container > picture {
		flex-shrink: 0;
	}
	.profile {
		border-radius: 50%;
		border: 2px solid var(--border);
		flex-shrink: 0;
		transition: width 0.3s ease;
		width: clamp(60px, 60px + 10vw, 200px);
	}
	ul {
		margin: 0;
		padding-left: 20px;
	}
	@media (min-width: 600px) {
		.container {
			flex: 0 0 auto;
			justify-content: center;
			column-gap: 20px;
		}

		.profile {
			display: block;
		}
	}

	@media (min-width: 1024px) {
		.container {
			flex-direction: row;
			align-items: center;
		}
		.profile {
			margin-top: 10px;
		}
	}
</style>
