<main class="drop-demo">
	<div class="drop-demo__item">
		<div class="bubble bubble--box">Box shadow</div>
		<p>Tail gets no shadow</p>
	</div>
	<div class="drop-demo__item">
		<div class="bubble bubble--drop">Drop shadow</div>
		<p>Shadow follows the shape</p>
	</div>
</main>

<style>
	.drop-demo {
		background: white;
		padding: 2rem 1rem;
		display: grid;
		gap: 1.4rem;
		grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
		max-width: 100%;
		color: black;
		--shadow: hsl(0deg 0% 0% / 0.2);
	}
	.drop-demo__item {
		display: grid;
		place-items: center;
		gap: 2rem;
	}
	.bubble {
		position: relative;
		padding: 1rem 1.2rem;
		background: hsl(0deg 0% 100%);
		border-radius: 10px;
	}
	.bubble::after {
		content: '';
		position: absolute;
		left: 26px;
		bottom: -20px;
		width: 34px;
		height: 24px;
		background: inherit;
		clip-path: polygon(50% 100%, 0 0, 100% 0);
	}
	.bubble--box {
		box-shadow:
			1px 2px 3px var(--shadow),
			2px 4px 6px var(--shadow),
			4px 8px 12px var(--shadow);
	}
	.bubble--drop {
		filter: drop-shadow(1px 2px 3px var(--shadow)) drop-shadow(2px 4px 6px var(--shadow))
			drop-shadow(4px 8px 12px var(--shadow));
	}
	p {
		margin: 0;
		font-size: 0.85rem;
		color: var(--small-text);
		text-align: center;
	}
</style>
