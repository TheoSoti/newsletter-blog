---
title: 'Write Cleaner CSS with Modern Properties'
description: 'Discover 5 powerful CSS features that simplify your code, like inset, scale, :is(), and more. Writing less CSS has never been easier.'
pubDate: 'May 18 2025'
---

import { Picture } from 'astro:assets';

<Picture
	src="/short/05-2025/shorthands-css.png"
	alt="5 shorthands for CSS"
	width="400"
	height="600"
	style="margin: 2em auto 1em auto; display: block; border: 4px solid var(--border)"
	format="webp"
/>

## Writing less CSS has never been easier.

Modern properties are doing some serious heavy lifting.

Here is a list of 5 CSS improvements

Instead of:

```css
div {
	transform: scale(2);
}
```

Just write:

```css
div {
	scale: 2;
}
```

Instead of:

```css
div {
	top: 0;
	right: 0;
	bottom: 0;
	left: 0;
}
```

Just write:

```css
div {
	inset: 0;
}
```

And there's more:

- `:is()` simplifies selectors.
- `min()` or `max()` combines constraints.
- `margin-inline` or `margin-block` makes layouts more flexible.

All of these tweaks add up to cleaner CSS.

---

I'm working on an ebook called "You Don't Need JavaScript".

To get notified when it launches 👉 https://theosoti.com/you-dont-need-js/
